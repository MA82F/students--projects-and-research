# Image Compression and Classification

یک پروژه یادگیری عمیق برای فشرده‌سازی تصاویر و طبقه‌بندی همزمان با استفاده از PyTorch و CompressAI.

## نگاه کلی

این پروژه شامل دو مدل عصبی مجزا است:
1. **مدل فشرده‌سازی**: با استفاده از Entropy Bottleneck برای فشرده‌سازی تصاویر
2. **مدل طبقه‌بندی**: برای تشخیص کلاس تصویر از روی نمایش فشرده شده

مدل‌ها بر روی ۱۰۱ کلاس مختلف آموزش دیده‌اند و قابلیت تشخیص طیف وسیعی از اشیاء از جمله حیوانات، وسایل نقلیه، آلات موسیقی و موارد دیگر را دارند.

## ویژگی‌های کلیدی

- ✅ فشرده‌سازی تصویر با کیفیت بالا
- ✅ طبقه‌بندی دقیق تصاویر
- ✅ محاسبه معیارهای کیفیت (PSNR, BPP)
- ✅ پردازش سریع
- ✅ ذخیره تصاویر بازسازی شده

## نصب و راه‌اندازی

### پیش‌نیازها

```bash
pip install torch torchvision
pip install compressai
pip install scikit-learn
pip install Pillow
pip install numpy
```

### کلون کردن پروژه

```bash
git clone <repository-url>
cd project
```

## ساختار پروژه

```
project/
├── models.py              # تعریف مدل‌های عصبی
├── infer.py              # اسکریپت استنتاج
├── README.md             # فایل راهنما
├── checkpoints/          # وزن‌های آموزش دیده شده
│   ├── augq5.pth.tar
│   └── focalq5.pth.tar
├── images/               # تصاویر نمونه
│   ├── airplane.jpg
│   └── camera.jpg
└── reconstructed/        # تصاویر بازسازی شده
    └── reconstructed.png
```

## استفاده

### اجرای استنتاج

```bash
python infer.py -w checkpoints/augq5.pth.tar -i images/airplane.jpg -o reconstructed/
```

### پارامترهای ورودی

- `-w, --weights`: مسیر فایل وزن‌های مدل (ضروری)
- `-i, --input`: مسیر تصویر ورودی (ضروری)  
- `-o, --output`: مسیر پوشه خروجی (ضروری)

### مثال خروجی

```
=== Inference Results ===
Input Image: images/airplane.jpg
Reconstructed Image Saved to: reconstructed/reconstructed.png
Predicted Class: airplanes (index: 5)
PSNR: 28.45 dB
Bitrate: 0.1234 bpp
Decoding Time: 45.2 ms
```

## کلاس‌های قابل تشخیص

مدل قادر به تشخیص ۱۰۱ کلاس مختلف است:

**حیوانات:** پلنگ، فیل، زرافه، پاندا، خرگوش، عقرب، ستاره دریایی و...

**وسایل نقلیه:** هواپیما، موتورسیکلت، ماشین، کشتی، هلیکوپتر و...

**آلات موسیقی:** گیتار الکتریک، پیانو، ساکسوفون، ویولن و...

**اشیاء روزمره:** دوربین، لپ‌تاپ، قیچی، چتر، ساعت و...

[لیست کامل کلاس‌ها در فایل `infer.py` موجود است]

## جزئیات فنی

### معماری مدل فشرده‌سازی
- **Encoder**: سه لایه کانولوشن با GDN activation
- **Decoder**: سه لایه deconvolution برای بازسازی
- **Entropy Bottleneck**: برای کدگذاری و فشرده‌سازی

### معماری مدل طبقه‌بندی
- **Residual Blocks**: بلوک‌های باقیمانده برای بهبود یادگیری
- **Pooling Layers**: کاهش ابعاد فضایی
- **Global Average Pooling**: تبدیل feature map به بردار ویژگی
- **Fully Connected**: لایه نهایی طبقه‌بندی

### پارامترهای مدل
- **N**: 128 (تعداد کانال‌های میانی)
- **M**: 192 (اندازه bottleneck)
- **Input Size**: 224×224 پیکسل
- **Classes**: 101 کلاس

## معیارهای ارزیابی

### PSNR (Peak Signal-to-Noise Ratio)
نسبت سیگنال به نویز که کیفیت بازسازی تصویر را نشان می‌دهد. مقادیر بالاتر بهتر هستند.

### BPP (Bits Per Pixel)  
میزان فشرده‌سازی که نشان‌دهنده تعداد بیت‌های مورد نیاز برای هر پیکسل است. مقادیر کمتر بهتر هستند.

## فایل‌های Checkpoint

پروژه شامل دو فایل وزن آموزش دیده است:

- `augq5.pth.tar`: مدل آموزش دیده با augmentation
- `focalq5.pth.tar`: مدل آموزش دیده با focal loss

## نیازمندی‌های سیستم

- **Python**: 3.7+
- **PyTorch**: 1.8+
- **CUDA**: برای پردازش GPU (اختیاری)
- **RAM**: حداقل 4GB
- **Storage**: حداقل 500MB برای checkpoint ها

## مثال‌های کاربردی

### پردازش تصویر تکی
```bash
python infer.py -w checkpoints/augq5.pth.tar -i my_image.jpg -o results/
```

### استفاده از مدل مختلف  
```bash
python infer.py -w checkpoints/focalq5.pth.tar -i images/camera.jpg -o output/
```

## نکات مهم

- تصاویر ورودی به صورت خودکار به اندازه 224×224 تغییر اندازه می‌یابند
- مدل بر روی GPU اجرا می‌شود در صورت دردسترس بودن
- تصاویر خروجی در فرمت PNG ذخیره می‌شوند
- زمان پردازش بستگی به اندازه تصویر و سخت‌افزار دارد

## توسعه و بهبود

برای توسعه بیشتر می‌توان:
- مدل‌های جدید با معماری‌های مختلف آزمایش کرد
- کلاس‌های جدید اضافه کرد
- روش‌های فشرده‌سازی پیشرفته‌تر استفاده کرد
- رابط کاربری گرافیکی ایجاد کرد

## مجوز

این پروژه تحت مجوز MIT منتشر شده است.

---

🚀 **ساخته شده با PyTorch و CompressAI**
